spring:
     jpa:
          properties:
               hibernate:
                    dialect: org.hibernate.dialect.PostgreSQLDialect
                    format_sql: "true"
          hibernate:
               ddl-auto: update
          show-sql: "true"
     datasource:
          url: jdbc:postgresql://ep-ancient-heart-adqsgk4v-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
          username: neondb_owner
          password: npg_D5vbm4icMYuf

springdoc:
     api-docs:
          path: /v3/api-docs
          enabled: true

     swagger-ui:
          path: /swagger-ui.html
          try-it-out-enabled: true
          operations-sorter: method
          tags-sorter: alpha
          display-request-duration: true
          disable-swagger-default-url: true

     show-actuator: false

server:
     port: "8084"
     forward-headers-strategy: framework
